import styled from 'styled-components';

export const StyledInput = styled.input`
  border: none;
  color: #3405A2;
  font-size: 16px;
  padding: 4px 14px;
  border-radius: 3px;
  text-align: center;
  box-sizing: border-box;

  &:hover, &:focus{
    outline: none;
  }
`;